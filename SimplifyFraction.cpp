#include <bits/stdc++.h>

using namespace std;

int main(void)
{
   string s, s1, s2;
   cin>>s;

    int i = 0;

    for(i = 0 ;i<s.length(); i++)
    {
        if(s[i] != '/' || s[i] != '*' || s[i] != '+' || s[i] != '-')
        {
            s2[i] == s[i];
            cout<<s2[i]<<endl;
        }
    }

   return 0;
}
