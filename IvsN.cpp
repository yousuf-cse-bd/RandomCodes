#include <iostream>
using namespace std;

int main(int argc, char const *argv[])
{
    /* code */
    int i, N = 10;
    for(i = 0; i<N; i++);
    (i == N) ? cout<<"Equal!"<<endl: cout<<"!Equal"<<endl;
    return 0;
}
